This system receives information from 3 topics and performs validation and updating as needed.

The system stores the information in Elasticsearch in three different indexes.
To run this system, you need to have docker installed on your computer.

1. Go to the folder where the project is located via the terminal.
2. Run the command     docker compose up --build
